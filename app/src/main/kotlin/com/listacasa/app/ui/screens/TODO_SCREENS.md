# Pantallas por crear aquí (Claude Code)

Cada una como su propio archivo .kt en esta carpeta:

- `JoinHouseholdScreen.kt` — sección 1.1 de SPEC.md
- `MainListScreen.kt` — sección 1.4
- `AddItemSheet.kt` — sección 1.5 (cámara + escaneo de código de barras)
- `ManageZonesScreen.kt` — sección 1.6

Componentes reutilizables (tarjeta de producto, chip de zona, barra de progreso)
van en `../components/`.

Borrar este archivo una vez creadas las pantallas reales.
